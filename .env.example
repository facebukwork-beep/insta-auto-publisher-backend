PORT=8787
GRAPH_API_VERSION=v23.0
APP_SECRET_KEY=change-this-to-a-long-random-secret

# Optional. On Render this can normally be omitted because the
# backend detects the public URL automatically.
# PUBLIC_BASE_URL=https://your-service.onrender.com

META_MIN_REQUEST_INTERVAL_SECONDS=10
META_RATE_LIMIT_BACKOFF_MINUTES=30
MAX_AUTO_RETRIES=8
